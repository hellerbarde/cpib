using System;
using System.Text;

namespace Compiler
{
  public partial class {{ line.name | capitalizefirstchar }}{{ entry.name | upper }} : I{{ line.name | capitalizefirstchar }}
  {
{%- for token in entry.entry %}
  {%- if token.type == "Nonterminal" %}
    public I{{ token.tokentype | capitalizefirstchar }} {{ token.name | lower }} { get; set; }
  {% elif token.type == "Terminal" %}
    public Tokennode {{ token.name | upper }} { get; set; } 
  {% else %}
    // Epsilon
  {% endif -%}
{% endfor %}
    public {{ line.name | capitalizefirstchar }}{{ entry.name | upper }}()
    {
    }

    public override String ToString()
    {
      return ("lalalal");
    }
  }
}

